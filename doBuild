mkdir -p `pwd`/.gradle


docker build \
   -t peer-build \
    -f Dockerfile.build \
    .


docker run \
   -u `id -u $USER` \
   -v `pwd`:/app \
   -t peer-build \
   ./gradlew clean testReport 
    
