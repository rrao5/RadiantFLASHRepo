mkdir -p `pwd`/.gradle


docker build \
   -t peer-build \
    -f Dockerfile.build \
    .


docker run \
   -u `id -u $USER` \
   -v `pwd`:/app \
   -t peer-build \
   /bin/bash -c "npm install; ./gradlew clean testReport"
    
